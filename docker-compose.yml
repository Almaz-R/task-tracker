version: '3.8'
services:
  app:
    build: .
    container_name: task-tracker-app
    restart: always
    environment:
      - KAFKA_BROKER=my-kafka:9092
      - DB_HOST=postgres-db
    ports:
      - "8000:8000"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge