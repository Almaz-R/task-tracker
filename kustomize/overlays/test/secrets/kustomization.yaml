apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../../base/secrets
patches:
- target:
    kind: Secret
    name: db-secret
  patch: |-
    - op: replace
      path: /stringData/DB_PASSWORD
      value: "password"