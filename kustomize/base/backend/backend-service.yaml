apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: task-tracker-test
spec:
  selector:
    app: backend-broken
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP