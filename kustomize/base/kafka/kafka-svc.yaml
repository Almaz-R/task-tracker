apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: default
spec:
  selector:
    app: my-kafka
  ports:
    - name: kafka-internal
      port: 9092
      targetPort: 9092
  type: ClusterIP