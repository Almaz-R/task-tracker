apiVersion: v1
kind: Service
metadata:
  name: prometheus-grafana
#  namespace: task-tracker-test
spec:
  selector:
    app: grafana
  ports:
    - name: http-web
      port: 80
      targetPort: 3000
  type: ClusterIP